# Local dev — vite proxy handles /api → localhost:5000, so leave this blank
VITE_API_URL=

# Production — set this to your Render backend URL in Vercel dashboard
# VITE_API_URL=https://your-api.onrender.com/api
